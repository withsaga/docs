---
title: "Quickstart"
description: "Create an account, add credits, and request your first expert review"
"og:title": "Quickstart | Experts Know Docs"
"og:description": "Create an account, add credits, and request your first expert review"
"twitter:title": "Quickstart | Experts Know Docs"
"twitter:description": "Create an account, add credits, and request your first expert review"
icon: "rocket"
---

## 1. Create an account

Create an account at [experts-know.vercel.app/signup](https://experts-know.vercel.app/signup). The signup flow returns an API key and creates a dashboard session.

Keep the API key somewhere safe. You can also view it later from **Dashboard -> Settings**.

## 2. Add credits

Agent-submitted reviews use prepaid credits. Add balance from **Dashboard -> Settings** or **Dashboard -> Credits**.

Web-submitted reviews do not require a prepaid balance. They create a Stripe Checkout session for the specific review request.

## 3. Request a review from the web app

Open the review form, choose a review domain and tier, add the question and context, then submit.

Standard priced requests move to Stripe Checkout. Custom requests enter `pending_pricing` while the team scopes the review.

## 4. Or connect an AI agent

Add the MCP server to Claude Code, Cursor, Windsurf, or another MCP client:

```json
{
  "mcpServers": {
    "experts-know": {
      "command": "npx",
      "args": ["-y", "experts-know-mcp"],
      "env": {
        "API_URL": "https://experts-know.vercel.app",
        "API_KEY": "your_api_key"
      }
    }
  }
}
```

Then ask your agent to follow this sequence:

1. `list_expert_capabilities`
2. `quote_expert_review`
3. `check_credit_balance`
4. `request_expert_review`
5. `check_expert_review_status`

## 5. Track the result

Open the dashboard to see request status, payment state, expert assignment, activity events, and completed reviews.

Agents can call `list_my_requests` or `check_expert_review_status` to retrieve the same information programmatically.
