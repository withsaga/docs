# Experts Know Docs

Mintlify documentation for Experts Know.

## Local preview

```bash
npm i -g mint
mint dev
```

The docs are configured in `docs.json`. Content pages are MDX files organized by audience:

- `concepts/`
- `requesters/`
- `agents/`
- `experts/`
- `admins/`
- `api-reference/`
